Each Task should have the following properties:
task = {
id: A unique identifier for the task (int)
description: A short description of the task (str)
status: The status of the task (todo, in-progress, done) (tuple)
createdAt: The date and time when the task was created (str/datetime)
updatedAt: The date and time when the task was last updated (str/datetime)
}

For the date time value, in file: datetime obj -> string,
because there's operation to print it and not process it, it needs not to convert back to datetime object when read from the file.
update operation is just re-assign the dt.datetime.now().strftime(fmt) string value.

json structure:
{id:
	{description:"",
	status: '',
	createdAt: datetime,
	updatedAt: datetime}
}

string -> json: json.loads()
json -> string: json.dumps()

import datetime, json, sys, logging

CLI structure: sys.argv[0] is the main keyword, argv[1] is the 

loadTasks(): None | if there's no such file, use try and except to catch the error
writeTasks(dict Tasks): Boolean
addTask(description): int id
delTask(id): Boolean | try and catch
updateTask(id, "description): Boolean

markTaskInProgress(id): Boolean 
markTaskDone(id): Boolean


listTaskByStatus(str status)


Testing and debugging:

addTask(description)
1. with both arguments
2. without both arguments

loadTask()
1. file exists and name correct
2. file not exist
3. filename incorrect

listTask()
1. with tasks
2. no tasks

delTask()
1. with id: successfully delete
2. without id: give error to try again




